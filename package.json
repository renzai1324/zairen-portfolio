{
  "name": "zairen-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute zairen-portfolio-db --remote --file=schema.sql"
  },
  "devDependencies": { "wrangler": "^4.43.0" }
}
